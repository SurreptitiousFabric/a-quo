# A Quo joined-lifecycle fixture v1

This is an inert synthetic Omarchy plugin fixture for the A Quo disposable
package-lifecycle evaluator. It contains no executable file and declares no
entry point. Its unknown `a-quo-evaluator-fixture` kind is intentionally not a
loadable Omarchy shell kind.

The archive is test input, not a useful plugin, release, safety finding, or
publisher identity.
