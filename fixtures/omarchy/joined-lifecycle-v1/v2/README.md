# A Quo joined-lifecycle fixture v2

This is the strictly newer inert synthetic Omarchy plugin fixture for the A
Quo disposable package-lifecycle evaluator. It contains no executable file and
declares no entry point. Its unknown `a-quo-evaluator-fixture` kind is
intentionally not a loadable Omarchy shell kind.

The changed documentation makes the v2 tree and archive distinct from v1. The
archive is test input, not a useful plugin, release, safety finding, or
publisher identity.
